import Banner from "~/components/banner/banner";

export default function Home() {
  // const hello = api.example.getAll.useQuery({ text: "from tRPC" });

  return (
    <>
      <main>
        <Banner />
      </main>
    </>
  );
}
